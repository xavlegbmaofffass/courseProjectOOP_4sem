<Window x:Class="FrontlineCardWarfare.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FrontlineCardWarfare"
        xmlns:views="clr-namespace:FrontlineCardWarfare.Views"
        mc:Ignorable="d"
        Title="Frontline: Card Warfare"
        Height="750"
        Width="1100"
        MinHeight="650"
        MinWidth="900"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        MouseLeftButtonDown="Window_MouseLeftButtonDown">
    <Window.Resources>
        <Style x:Key="WindowControlButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#A0A0B0"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Width" Value="44"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="4" Padding="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <Border x:Name="MainBorder" Background="#0A0A12" CornerRadius="16" BorderBrush="#3E3E5A" BorderThickness="2">
        <Border.Effect>
            <DropShadowEffect Color="#000000" BlurRadius="20" ShadowDepth="5" Opacity="0.5"/>
        </Border.Effect>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Border Grid.Row="0" Background="#0F0F1A" CornerRadius="16,16,0,0" Height="42" BorderBrush="#1E1E32" BorderThickness="0,0,0,1">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0" Orientation="Horizontal" Margin="16,0,0,0">
                        <TextBlock Text="FRONTLINE: CARD WARFARE" Foreground="#6A6A8A" FontSize="12" FontWeight="SemiBold" VerticalAlignment="Center" FontFamily="Consolas"/>
                    </StackPanel>
                    <StackPanel Grid.Column="2" Orientation="Horizontal" Margin="0,0,8,0">
                        <Button x:Name="MinimizeButton" Content="─" Style="{StaticResource WindowControlButton}" Click="MinimizeButton_Click" MouseEnter="WindowControlButton_MouseEnter" MouseLeave="WindowControlButton_MouseLeave"/>
                        <Button x:Name="MaximizeButton" Content="□" Style="{StaticResource WindowControlButton}" Click="MaximizeButton_Click" MouseEnter="WindowControlButton_MouseEnter" MouseLeave="WindowControlButton_MouseLeave"/>
                        <Button x:Name="CloseButton" Content="✕" Style="{StaticResource WindowControlButton}" Click="CloseButton_Click" MouseEnter="CloseButton_MouseEnter" MouseLeave="CloseButton_MouseLeave"/>
                    </StackPanel>
                </Grid>
            </Border>
            <Border Grid.Row="1" CornerRadius="0,0,16,16" ClipToBounds="True">
                <ContentControl x:Name="MainContent" />
            </Border>
        </Grid>
    </Border>
</Window>
