<UserControl x:Class="FrontlineCardWarfare.Views.CollectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:data="clr-namespace:FrontlineCardWarfare.Data"
             xmlns:conv="clr-namespace:FrontlineCardWarfare.Converters"
             mc:Ignorable="d"
             d:DesignHeight="750" d:DesignWidth="1050">
    <UserControl.Resources>
        <conv:CardTypeToColorConverter x:Key="CardTypeToColorConverter"/>
        <conv:CardTypeToDisplayConverter x:Key="CardTypeToDisplayConverter"/>
        <conv:ImagePathConverter x:Key="ImagePathConverter"/>
        <conv:CardActionDescriptionConverter x:Key="CardActionDescriptionConverter"/>

        <Style x:Key="ModernTextBox" TargetType="TextBox">
            <Setter Property="Background" Value="#0F0F1A"/>
            <Setter Property="Foreground" Value="#E0E0FF"/>
            <Setter Property="BorderBrush" Value="#1E1E32"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="14,12"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="CaretBrush" Value="#00D9FF"/>
            <Setter Property="SelectionBrush" Value="#00D9FF40"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>

        <Style x:Key="ModernComboBox" TargetType="ComboBox">
            <Setter Property="Background" Value="#0F0F1A"/>
            <Setter Property="Foreground" Value="#E0E0FF"/>
            <Setter Property="BorderBrush" Value="#1E1E32"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton x:Name="ToggleButton"
                                          IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                          Background="{TemplateBinding Background}"
                                          BorderBrush="{TemplateBinding BorderBrush}"
                                          BorderThickness="{TemplateBinding BorderThickness}"
                                          Foreground="{TemplateBinding Foreground}"
                                          Padding="{TemplateBinding Padding}"
                                          Cursor="Hand">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border Background="{TemplateBinding Background}"
                                                BorderBrush="{TemplateBinding BorderBrush}"
                                                BorderThickness="{TemplateBinding BorderThickness}"
                                                CornerRadius="10">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="32"/>
                                                </Grid.ColumnDefinitions>
                                                <ContentPresenter Grid.Column="0"
                                                                  Content="{Binding SelectionBoxItem, RelativeSource={RelativeSource AncestorType=ComboBox}}"
                                                                  Margin="{TemplateBinding Padding}"
                                                                  VerticalAlignment="Center"
                                                                  HorizontalAlignment="Left"/>
                                                <Path Grid.Column="1"
                                                      x:Name="Arrow"
                                                      Fill="#8A8AA0"
                                                      Data="M 0 0 L 4 4 L 8 0 Z"
                                                      HorizontalAlignment="Center"
                                                      VerticalAlignment="Center"
                                                      Stretch="Uniform"
                                                      Width="10"
                                                      Height="6"/>
                                            </Grid>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="Arrow" Property="Fill" Value="#E0E0FF"/>
                                            </Trigger>
                                            <Trigger Property="IsChecked" Value="True">
                                                <Setter TargetName="Arrow" Property="Data" Value="M 0 4 L 4 0 L 8 4 Z"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>
                            <Popup x:Name="Popup"
                                   Placement="Bottom"
                                   IsOpen="{TemplateBinding IsDropDownOpen}"
                                   AllowsTransparency="True"
                                   Focusable="False"
                                   PopupAnimation="Slide">
                                <Grid x:Name="DropDown"
                                      SnapsToDevicePixels="True"
                                      MinWidth="{TemplateBinding ActualWidth}"
                                      MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                    <Border x:Name="DropDownBorder"
                                            Background="#0F0F1A"
                                            BorderBrush="#1E1E32"
                                            BorderThickness="1"
                                            CornerRadius="10">
                                        <ScrollViewer SnapsToDevicePixels="True">
                                            <ItemsPresenter KeyboardNavigation.DirectionalNavigation="Contained"/>
                                        </ScrollViewer>
                                    </Border>
                                </Grid>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Resources>
                <Style TargetType="ComboBoxItem">
                    <Setter Property="Foreground" Value="#E0E0FF"/>
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="Padding" Value="12,10"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ComboBoxItem">
                                <Border Background="{TemplateBinding Background}"
                                        CornerRadius="6"
                                        Padding="{TemplateBinding Padding}">
                                    <ContentPresenter/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#1A1A2E"/>
                                    </Trigger>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter Property="Background" Value="#1E1E3A"/>
                                        <Setter Property="Foreground" Value="#00D9FF"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </Style.Resources>
        </Style>

        <Style x:Key="PrimaryActionButton" TargetType="Button">
            <Setter Property="Background">
                <Setter.Value>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                        <GradientStop Color="#00A8CC" Offset="0"/>
                        <GradientStop Color="#0077B6" Offset="1"/>
                    </LinearGradientBrush>
                </Setter.Value>
            </Setter>
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="20,12"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="10"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Opacity" Value="0.85"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SecondaryActionButton" TargetType="Button">
            <Setter Property="Background" Value="#1A1A2E"/>
            <Setter Property="Foreground" Value="#8A8AA0"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Padding" Value="16,12"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#2A2A44"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="10"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="BorderBrush" Value="#4A4A6A"/>
                                <Setter Property="Foreground" Value="#E0E0FF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <DataTemplate DataType="{x:Type data:Card}">
            <Border x:Name="cardBorder"
                    Background="#0F0F1A"
                    CornerRadius="16"
                    Padding="16"
                    Margin="10"
                    BorderBrush="#1A1A2E"
                    BorderThickness="1"
                    Width="230"
                    Cursor="Hand"
                    RenderTransformOrigin="0.5,0.5">
                <Border.RenderTransform>
                    <ScaleTransform ScaleX="1" ScaleY="1"/>
                </Border.RenderTransform>
                <Border.Effect>
                    <DropShadowEffect Color="#000000" BlurRadius="20" ShadowDepth="4" Opacity="0.4"/>
                </Border.Effect>
                <Border.Triggers>
                    <EventTrigger RoutedEvent="Mouse.MouseEnter">
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation Storyboard.TargetName="cardBorder"
                                                 Storyboard.TargetProperty="(Border.RenderTransform).(ScaleTransform.ScaleX)"
                                                 To="1.03"
                                                 Duration="0:0:0.25">
                                    <DoubleAnimation.EasingFunction>
                                        <QuadraticEase EasingMode="EaseOut"/>
                                    </DoubleAnimation.EasingFunction>
                                </DoubleAnimation>
                                <DoubleAnimation Storyboard.TargetName="cardBorder"
                                                 Storyboard.TargetProperty="(Border.RenderTransform).(ScaleTransform.ScaleY)"
                                                 To="1.03"
                                                 Duration="0:0:0.25">
                                    <DoubleAnimation.EasingFunction>
                                        <QuadraticEase EasingMode="EaseOut"/>
                                    </DoubleAnimation.EasingFunction>
                                </DoubleAnimation>
                                <ColorAnimation Storyboard.TargetName="cardBorder"
                                                Storyboard.TargetProperty="(Border.BorderBrush).(SolidColorBrush.Color)"
                                                To="#00D9FF"
                                                Duration="0:0:0.25">
                                    <ColorAnimation.EasingFunction>
                                        <QuadraticEase EasingMode="EaseOut"/>
                                    </ColorAnimation.EasingFunction>
                                </ColorAnimation>
                            </Storyboard>
                        </BeginStoryboard>
                    </EventTrigger>
                    <EventTrigger RoutedEvent="Mouse.MouseLeave">
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation Storyboard.TargetName="cardBorder"
                                                 Storyboard.TargetProperty="(Border.RenderTransform).(ScaleTransform.ScaleX)"
                                                 To="1"
                                                 Duration="0:0:0.25">
                                    <DoubleAnimation.EasingFunction>
                                        <QuadraticEase EasingMode="EaseOut"/>
                                    </DoubleAnimation.EasingFunction>
                                </DoubleAnimation>
                                <DoubleAnimation Storyboard.TargetName="cardBorder"
                                                 Storyboard.TargetProperty="(Border.RenderTransform).(ScaleTransform.ScaleY)"
                                                 To="1"
                                                 Duration="0:0:0.25">
                                    <DoubleAnimation.EasingFunction>
                                        <QuadraticEase EasingMode="EaseOut"/>
                                    </DoubleAnimation.EasingFunction>
                                </DoubleAnimation>
                                <ColorAnimation Storyboard.TargetName="cardBorder"
                                                Storyboard.TargetProperty="(Border.BorderBrush).(SolidColorBrush.Color)"
                                                To="#1A1A2E"
                                                Duration="0:0:0.25">
                                    <ColorAnimation.EasingFunction>
                                        <QuadraticEase EasingMode="EaseOut"/>
                                    </ColorAnimation.EasingFunction>
                                </ColorAnimation>
                            </Storyboard>
                        </BeginStoryboard>
                    </EventTrigger>
                </Border.Triggers>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="240"/>
                        <RowDefinition Height="58"/>
                        <RowDefinition Height="66"/>
                        <RowDefinition Height="48"/>
                    </Grid.RowDefinitions>

                    <!-- Изображение карты -->
                    <Border Grid.Row="0"
                            Background="#080810"
                            CornerRadius="12"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            Width="Auto"
                            Height="Auto">
                        <Image Source="{Binding ImagePath, Converter={StaticResource ImagePathConverter}}"
                               Stretch="Uniform"
                               MaxHeight="240"
                               MaxWidth="240"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center"/>
                    </Border>

                    <!-- Название + тип -->
                    <StackPanel Grid.Row="1" 
                                HorizontalAlignment="Center" 
                                VerticalAlignment="Center"
                                Margin="0,4,0,4">
                        <TextBlock Text="{Binding Name}"
                                   FontSize="19"
                                   FontWeight="Bold"
                                   Foreground="#E0E0FF"
                                   TextAlignment="Center"
                                   TextWrapping="Wrap"
                                   TextTrimming="CharacterEllipsis"
                                   MaxHeight="34"
                                   LineHeight="17"/>
                        <TextBlock Text="{Binding CardType, Converter={StaticResource CardTypeToDisplayConverter}}"
                                   FontSize="15"
                                   Foreground="{Binding CardType, Converter={StaticResource CardTypeToColorConverter}}"
                                   HorizontalAlignment="Center"
                                   FontWeight="SemiBold"
                                   Margin="0,2,0,0"/>
                    </StackPanel>

                    <!-- Описание действия -->
                    <Border Grid.Row="2" 
                            Background="#08081040"
                            CornerRadius="8"
                            Padding="6,4"
                            Margin="0,2,0,4">
                        <TextBlock Text="{Binding Converter={StaticResource CardActionDescriptionConverter}}"
                                   FontSize="15"
                                   Foreground="#9A9AB0"
                                   TextAlignment="Center"
                                   TextWrapping="Wrap"
                                   TextTrimming="WordEllipsis"
                                   VerticalAlignment="Center"
                                   LineHeight="15"/>
                    </Border>

                    <!-- Статы -->
                    <UniformGrid Grid.Row="3" Columns="3" HorizontalAlignment="Stretch" VerticalAlignment="Center">
                        <Border Background="#00A8CC15" CornerRadius="8" Padding="4,3" Margin="2,0" BorderBrush="#00A8CC30" BorderThickness="1">
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                <TextBlock Text="⚔" FontSize="15" VerticalAlignment="Center" Margin="0,0,2,0" Foreground="#00D9FF"/>
                                <TextBlock Text="{Binding Attack}" FontSize="16" Foreground="#00D9FF" FontWeight="Bold" FontFamily="Consolas"/>
                            </StackPanel>
                        </Border>
                        <Border Background="#E8456015" CornerRadius="8" Padding="4,3" Margin="2,0" BorderBrush="#E8456030" BorderThickness="1">
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                <TextBlock Text="❤" FontSize="15" VerticalAlignment="Center" Margin="0,0,2,0" Foreground="#E84560"/>
                                <TextBlock Text="{Binding Health}" FontSize="16" Foreground="#E84560" FontWeight="Bold" FontFamily="Consolas"/>
                            </StackPanel>
                        </Border>
                        <Border Background="#FFD70015" CornerRadius="8" Padding="4,3" Margin="2,0" BorderBrush="#FFD70030" BorderThickness="1">
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                <TextBlock Text="🏹" FontSize="15" VerticalAlignment="Center" Margin="0,0,2,0" Foreground="#FFD700"/>
                                <TextBlock Text="{Binding Range}" FontSize="16" Foreground="#FFD700" FontWeight="Bold" FontFamily="Consolas"/>
                            </StackPanel>
                        </Border>
                    </UniformGrid>
                </Grid>
            </Border>
        </DataTemplate>
    </UserControl.Resources>

    <Grid>
        <!-- Фон -->
        <Border>
            <Border.Background>
                <RadialGradientBrush Center="0.5,0.5" GradientOrigin="0.3,0.3">
                    <GradientStop Color="#0D0D1A" Offset="0"/>
                    <GradientStop Color="#080810" Offset="0.7"/>
                    <GradientStop Color="#050508" Offset="1"/>
                </RadialGradientBrush>
            </Border.Background>
        </Border>

        <!-- Контент -->
        <Grid Margin="24">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Заголовок -->
            <StackPanel Grid.Row="0" Margin="0,0,0,16">
                <TextBlock Text="КОЛЛЕКЦИЯ КАРТ"
                           FontSize="17"
                           FontWeight="Bold"
                           Foreground="#4A4A6A"
                           FontFamily="Consolas"
                           Margin="0,0,0,8"/>
                <TextBlock Text="Все доступные карты"
                           FontSize="28"
                           FontWeight="Bold"
                           Foreground="#E0E0FF"/>
            </StackPanel>

            <!-- Поиск и фильтры -->
            <Border Grid.Row="1" Background="#0F0F1A" CornerRadius="12" Padding="16" Margin="0,0,0,16" BorderBrush="#1A1A2E" BorderThickness="1">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <!-- Легенда статов -->
                    <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock Text="⚔ Атака" FontSize="17" Foreground="#00D9FF" Margin="0,0,12,0" VerticalAlignment="Center"/>
                        <TextBlock Text="❤ Здоровье" FontSize="17" Foreground="#E84560" Margin="0,0,12,0" VerticalAlignment="Center"/>
                        <TextBlock Text="🏹 Дальность" FontSize="17" Foreground="#FFD700" Margin="0,0,12,0" VerticalAlignment="Center"/>
                    </StackPanel>
                    
                    <!-- Поиск + фильтр -->
                    <StackPanel Grid.Column="2" Orientation="Horizontal">
                        <ComboBox ItemsSource="{Binding CardTypeFilters}"
                                  SelectedIndex="{Binding SelectedTypeIndex}"
                                  Width="160"
                                  Margin="0,0,12,0"
                                  Style="{StaticResource ModernComboBox}"/>
                        <Grid Width="220" Margin="0,0,12,0">
                            <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                                     Style="{StaticResource ModernTextBox}"/>
                            <TextBlock Text="Введите название карты..." 
                                       Foreground="#4A4A6A"
                                       IsHitTestVisible="False"
                                       VerticalAlignment="Center"
                                       Margin="14,0,0,0">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding SearchText}" Value="">
                                                <Setter Property="Visibility" Value="Visible"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </Grid>
                        <Button Content="Найти"
                                Command="{Binding FilterCommand}"
                                Style="{StaticResource PrimaryActionButton}"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Список карт -->
            <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto"
                          HorizontalScrollBarVisibility="Disabled"
                          Margin="0,0,0,16">
                <ItemsControl ItemsSource="{Binding Cards}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel HorizontalAlignment="Center"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                </ItemsControl>
            </ScrollViewer>

            <!-- Кнопки -->
            <StackPanel Grid.Row="3" Orientation="Horizontal">
                <Button Content="Назад в меню"
                        Command="{Binding BackToMenuCommand}"
                        Style="{StaticResource SecondaryActionButton}"/>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>